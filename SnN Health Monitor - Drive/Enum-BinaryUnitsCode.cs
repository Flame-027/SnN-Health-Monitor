
namespace Drive
{
    public enum BinaryUnitsCode
    {
        KB = 0,
        MB = 1,
        GB = 2,
        TB = 3
    }

    // The enum holds index's for a list instead of just holding the values directly as the TB value is too large for an enum
}
